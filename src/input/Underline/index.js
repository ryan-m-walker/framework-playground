import Underline from '../../shared/Underline';

import './_input-underline.scss';

export default class InputUnderline extends Underline {
  constructor(underline, parent) {
    super(underline, parent);
  }
}